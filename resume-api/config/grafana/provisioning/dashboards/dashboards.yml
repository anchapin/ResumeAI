# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'Resume API Dashboards'
    orgId: 1
    folder: 'Resume API'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
